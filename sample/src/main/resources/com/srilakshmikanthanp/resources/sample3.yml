name: Sample3Yml

resources:
  # inline is the default type, so we can omit it and directly provide the command
  print: echo "Hello, World"

  config:
    file: sample1.yml

  echo:
    inline: echo "Hello, World"
