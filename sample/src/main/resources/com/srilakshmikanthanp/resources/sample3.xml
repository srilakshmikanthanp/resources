<resources name="Sample3Xml">
	<resource name="echo">
		<inline>echo "Hello, World"</inline>
	</resource>

  <!-- Inline is default, so it can be omitted -->
  <resource name="print">
    echo "Hello, World"
  </resource>

	<resource name="config">
		<file path="sample1.xml"/>
	</resource>
</resources>
